apiVersion: v1
kind: Service
metadata:
  name: digitalclock
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: digitalclock
  ports:
    - name: http
      port: 80
      targetPort: 80
